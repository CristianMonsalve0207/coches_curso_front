export const environment = {
  //el enviroment sirve para crear rutas de ambiente, por ejemplo, si estamos en desarrollo, produccion, etc
  production: false,
  //esta es la ruta de las apis del back que se van a consumir
  apiUrl: 'http://localhost:8080'
};
